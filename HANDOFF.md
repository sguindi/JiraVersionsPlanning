# planJira — Claude Handoff Summary
**Last updated:** 2026-09-03 | **Deployed version:** v2.91.0 (dev) / v2.1.0 (production)

---

## What This Project Is

**planJira** is a visual project planning app built on **Atlassian Forge** (Custom UI). It runs as a global page inside Jira. Tech stack: React 18, Recharts, date-fns v3, `@forge/bridge` for Jira API calls (no backend resolver).

**Repo root:** `c:\Users\sharonc\OneDrive - SYM SERVICIOS INTEGRALES SA DE CV\Myapps\planJira`  
**App entry:** `static/app/src/App.js`  
**Deploy command:** `cd static/app && npm run build && cd ../.. && forge deploy`

---

## Key Files

| File | Purpose |
|---|---|
| `static/app/src/components/VersionPlanningView.js` | Main planning view — all Gantt/timeline logic |
| `static/app/src/utils/planning.js` | Pure utility functions: `cascadePlan`, `detectConflicts`, `calcEndDate`, `calcDays`, `calcQaBugFixDays`, `findCriticalPath`, `addWorkingDays`, `buildWorkingDays` |
| `static/app/src/components/TimelineView.js` | Gantt chart (Recharts) for viewing issue timelines |
| `static/app/src/components/CalendarView.js` | Drag-to-schedule calendar view |
| `static/app/src/components/TeamView.js` | Developer workload heatmap |
| `static/app/src/__tests__/utils.test.js` | 23 unit tests for planning utils |
| `static/app/src/__tests__/components.test.js` | 10 smoke tests for components |

---

## Current State of VersionPlanningView

### Planning Modes
The view has **three planning modes** toggled in the toolbar:

**Draft mode (purple):**
- Work at the **epic level** — drag epics onto the timeline
- Duration = `roughEst (hours) ÷ assignedDevs ÷ HOURS_PER_DAY`
- Auto-dependency: assigning the same developer to multiple epics auto-creates a dependency chain
- Each epic bar shows 3 segments: dev bar (developer colors) + QA bar (yellow) + bug-fix bar (orange)
- Code Freeze line + stabilization shaded region + Delivery line auto-computed on the SVG

**Final mode (blue):**
- Work at the **story/task level** — drag individual stories
- Uses rough estimates or "Children Sum" (sum of child original estimates)
- Epic rows show a summary bar spanning all placed stories, plus QA + bugfix extension bars

**Epic Timeline mode (green):**
- Pick one epic from the toolbar dropdown (`focusEpicKey` state) — the table/timeline narrows to just that epic
- 3-level hierarchy: epic (summary bar, read-only) → stories (draggable, expandable via `expandedStories` state) → subtasks (draggable, revealed when a story is expanded)
- Subtasks are fully schedulable like stories: own bar, editable Est, assignable devs — `getBarProps`/`updateIssueEntry`/`roughMap` are all generic per-issue-key so no special-casing was needed there
- Reuses the Final mode's Rough Est / Children Sum toggle, resizable/sortable columns, and deferred Est-save
- `getSubtasksForStories` (bridge.js) now also fetches the Rough Estimation custom field, and `buildRoughMap`/`buildMissingEstMap` include subtask-level estimates
- **Auto dev assignment from Jira assignee**: a `useEffect` (keyed on `focusEpicKey`/`storiesByEpic`/`subtasksByStory`/`plan.issues`) walks every story+subtask under the focused epic and, if it isn't already assigned a dev placeholder, creates/reuses one from its Jira `fields.assignee` via `ensurePlaceholderForAssignee(accountId, displayName)` (useVersionPlan.js) — that placeholder's `id` is deterministic (`'acc_' + accountId`), so it can be used immediately without waiting on the state update to land. Never overrides a manual assignment. If a subtask has no assignee of its own, it inherits the parent story's assignee and gets `entry.borrowedFromParent = true` — rendered at reduced opacity (chip in the Assigned column + Gantt bar) so it reads as "not really assigned to them."
- **Cascading subtask scheduling**: dropping a story/task with subtasks onto the timeline (`placeOnTimeline` in `handleTimelineClick` and the right-panel `onDrop`) routes to `placeStoryAndSubtasks(storyKey, startDate)`, which places the story, then chains its subtasks back-to-back — each starting at `nextWorkDay(prevEnd)`, each depending on the previous one (`dependencies: [prevKey]`), each using its own Jira assignee or the parent story's (flagged `borrowedFromParent`) if unassigned. Re-dropping the story re-runs the whole cascade from the new date.
- **Issue type icon + status badge**: the Key cell now shows the Jira issue-type icon (`fields.issuetype.iconUrl`) and a small colored status-initials badge (`statusInitials`/`statusColors`, keyed off `fields.status.statusCategory.key`) next to every row's key, in all three planning modes.
- **Status-driven auto-timeline** (Epic Timeline mode only): a `useEffect` scans every story+subtask under the focused epic; any issue whose status is NOT in `NOT_STARTED_STATUSES` (`reopened`/`to do`/`blocked`) and doesn't already have a `startDate` gets its changelog fetched once (`getIssueChangelog`, bridge.js — cached in `changelogCache` state to avoid refetching) and parsed (`extractStatusDates`) for the first transition into "In Progress" (→ `startDate`) and the first "Ready for Deployment" after it (→ `entry.actualEndDate`). `getBarProps` renders a bar with `entry.actualEndDate` as a fixed end instead of an estimate-based duration, marked with a green border + "✓" and `bar.isActual = true`. Never overrides an already-placed issue.

### Left Table (Key / Summary / Est / Assigned / QA d / Days)
- **Resizable**: drag the right edge of any header cell (`ColResizer` component, module-level) — widths live in `colWidths` state.
- **Sortable**: click a header to sort by that column (`sortCol`/`sortDir` state, `getSortValue` module-level fn). In Final mode, epics sort as groups and each epic's expanded child stories sort among themselves — hierarchy is preserved, not flattened. `sortedRows` (derived from `rows`) drives both the left table and the right timeline/arrows so row order stays in sync.
- **Est editing**: clicking Est opens an inline input. On blur/Enter it updates `localRoughEst` + marks the key in `dirtyEstKeys` (● shown next to the value) but does **not** call the Jira API. Pending edits are only pushed via `updateRoughEstimation` when the user clicks "Save to Jira" (`handleSave`), which then clears `dirtyEstKeys`.

### Key State Variables (in the main component)
```js
planningMode       // 'draft' | 'final'
bugFixPct          // % of dev time for bug fixes (default 20)
codeFreezeDays     // working days from last epic end to code freeze (default 5)
stabilizationDays  // working days of stabilization after code freeze (default 10)
qaMap              // { [epicKey]: qaHours } — QA hours per epic (input by PM)
colWidths          // { key, summary, est, assigned, qa, days } — resizable column widths
dirtyEstKeys       // Set of issue keys with unsaved Est edits (localRoughEst), pending "Save to Jira"
```

### Auto-computed dates (Draft mode only)
```js
lastEffectiveEnd    // last epic's dev+QA+bugfix end date
codeFreezeDate      // lastEffectiveEnd + codeFreezeDays
stabilizationEndDate // codeFreezeDate + stabilizationDays (= Final Delivery)
```

### Bar Rendering Architecture
- **Draft epics:** `getBarProps(key)` → own startDate + devs. Then IIFE computes `_qaDays`/`_bfDays` via `calcQaBugFixDays`. Renders 3 `<div>`s inside a React fragment.
- **Final epics:** `getEpicSummaryBarProps(key, ...)` → spans placed stories. Same IIFE pattern adds QA+bugfix divs after.
- **Stories/tasks:** `getBarProps(key)` → standard draggable bar.
- **Dependency arrows:** SVG `<path>` elements. In Draft mode, arrows leave from end of QA+bugfix bar: `sourceBar.left + (durationDays + qaDays + bugFixDays) * DAY_WIDTH - 2`.

### SVG Overlay Layer (Draft mode)
Rendered after all bars, before `</svg>`:
1. Blue shaded `<rect>` for stabilization region
2. Dark dashed `<line>` + label for Code Freeze
3. Green dashed `<line>` + label for Delivery

### DeliveryReport Component
```jsx
<DeliveryReport
  computedPlan={computedPlan}
  roughMap={roughMap}
  planName={...}
  mode={planningMode}
  codeFreezeDate={planningMode === 'draft' ? codeFreezeDate : null}
  finalDeliveryDate={planningMode === 'draft' ? stabilizationEndDate : null}
/>
```
Shows: Start / Dev Complete / Duration / Code Freeze / Final Delivery cards + milestones + team utilization % bars + critical path chain.  
Has a **"⎘ Copy" button** that copies a Markdown-formatted report to clipboard (turns green "✓ Copied" for 2 seconds).

---

## Constants
```js
HOURS_PER_DAY = 6    // working hours per day (in planning.js)
DAY_WIDTH = 50       // pixels per working day (in VersionPlanningView.js)
ROW_HEIGHT           // height of each Gantt row
HEADER_H             // SVG header height
```

---

## Key Utility Functions (planning.js)

```js
calcQaBugFixDays(roughHours, devCount, qaHours, bugFixPct)
  → { qaDays, bugFixDays, totalExtra }
  // qaHours → ceil(qaHours / HOURS_PER_DAY) = QA days
  // bugFixPct → (roughHours * pct/100) / devs / HOURS_PER_DAY = bugfix days

cascadePlan(plan, roughMap, opts = { qaMap, bugFixPct })
  // Topological sort, propagates start dates using EFFECTIVE end dates
  // (dev + QA + bugfix time) when chaining dependencies
  // GOTCHA: for ANY issue with a non-empty `dependencies` array, cascadePlan
  // unconditionally overwrites its startDate from the dependency's end date on
  // every call — it does not know the difference between "not yet placed" and
  // "user just manually moved/removed this". Any code path that lets a user
  // directly set/clear an issue's startDate (× remove, drag-to-date, click-to-place)
  // MUST also clear that issue's own `dependencies` in the same updateIssueEntry call,
  // or the manual change is silently overwritten on the next render. See
  // placeOnTimeline() and the two "Remove from timeline" × buttons in
  // VersionPlanningView.js for the pattern.

detectConflicts(plan, roughMap, opts = { qaMap, bugFixPct })
  // Returns array of { placeholder, source, target } for overlapping epic assignments

addWorkingDays(dateStr, n)  // skip weekends
buildWorkingDays(fromStr, count)  // returns array of working day strings
```

---

## Version History (this session)

| Version | What changed |
|---|---|
| v2.24.0 | Initial commit (Calendar, Timeline, Team, basic Version Planning) |
| v2.25.0 | Draft/Final planning modes, auto-dependency chaining, DeliveryReport |
| v2.26.0 | Fix: epic drag-and-drop not working in Draft mode |
| v2.27.0 | QA input column, QA+bugfix bar extensions on Draft epics, code freeze/stabilization SVG overlays, DeliveryReport date cards |
| v2.28.0 | Final mode QA+bugfix bars, dependency arrows from effective end, Copy Report button |
| v2.29.0 | QA fixes: arrow -2px alignment, console.warn for OOB stabilizationEndDate |
| v2.30.0 | Version selector dropdowns (both screens) now show only unreleased versions |
| v2.31.0 | Backlog panel's version filter dropdown (EpicHierarchyPanel) also filtered to unreleased-only |
| v2.32.0 | All version dropdowns also exclude archived versions (added `archived` field to bridge.js version fetch) |
| v2.33.0 | Left table columns (Key/Summary/Est/Assigned/QA/Days) are resizable (drag right edge) and sortable (click header); Est edits are held locally (● marks unsaved) and only persisted to Jira via "Save to Jira" |
| v2.34.0 | Tab order changed to Version Planning, Calendar, Timeline, Team; only Version Planning is enabled (default tab), the other three are grayed out/disabled ("Coming soon") until finished |
| v2.35.0 | New "Epic Timeline" planning mode: pick one epic from a dropdown and see just its stories (expandable to subtasks) on a dedicated timeline. Subtasks are fully schedulable (draggable bars, editable Est, assignable devs) — same resizable/sortable columns and deferred Est-save as Draft/Final. Subtasks now fetch the Rough Estimation field too. |
| v2.36.0 | Epic Timeline: dev placeholders now auto-derive from each issue's Jira assignee (never overrides a manual pick); dropping a story/task with subtasks on the timeline auto-schedules all its subtasks back-to-back, each depending on the previous one, each using its own assignee or falling back to the parent's — quick QA/dev scaffolding for one epic. |
| v2.37.0 | Issue-type icon + status-initials badge next to Key (all modes). Epic Timeline: subtasks that inherit the parent's assignee render at reduced opacity (chip + bar). New status-driven auto-timeline: issues past "not started" (not Reopened/To Do/Blocked) get real start/end dates pulled from Jira's changelog (In Progress → Ready for Deployment), rendered with a green border + ✓. |
| v2.38.0 | New "⤢ Maximize" toolbar button (`maximized` state) — overlays the whole planning view as `position: fixed` full-viewport, hiding the surrounding project-selector/tab chrome so the left table + timeline get the full screen to show all stories/subtasks. "⤡ Restore" toggles back to the normal in-page layout. |
| v2.39.0 | Epic Timeline: "Auto-schedule" now only touches issues still Reopened/To Do/Blocked (anything further along keeps its real status-history dates), processes them in **key order**, and chains a dependency onto the previous issue sharing the same assignee — so moving one shifts the rest of that dev's queue. Draft/Final mode's Auto-schedule behavior is unchanged. |
| v2.40.0 | "⤢ Maximize" now also hides the secondary panels (developer placeholders row, overlap/conflict/missing-estimate banners, milestones list, Delivery Report) so the left table + timeline take the full maximized screen. "⤡ Restore" brings them all back. |
| v2.41.0 | Fixed root cause of "× doesn't work" / "can't move a placed bar": `cascadePlan` always recomputes `startDate` from `dependencies`, so any manual removal or re-drag was immediately overwritten by the dependency chain. Both × buttons and `placeOnTimeline`'s manual-placement path now also clear `dependencies` on that issue, so manual actions win. Left-table rows for issues already on the timeline now get a light green background (`#E3FCEF`). |
| v2.42.0 | Issues with real dates from Jira status history (`entry.actualEndDate` set) are now locked — not draggable (left-table row or Gantt bar), not clickable-to-place, shown with a 🔒 instead of the row expand caret area / bar's ✓ badge, cursor `not-allowed`. `placeOnTimeline` itself also refuses placement for locked issues as a defense-in-depth check. Removing via × still works (clears startDate but not actualEndDate). |
| v2.43.0 | Fixed Epic Timeline "Auto-schedule" building its candidate list from `rows` — which only contains subtasks of *currently expanded* stories — so collapsed stories' subtasks were silently skipped from assignee-based dependency chaining. Now builds candidates directly from `storiesByEpic`/`subtasksByStory` for the focused epic, regardless of expand state. |
| v2.44.0 | New "Clear all" toolbar button (`clearAllScheduling`) — unschedules everything in the current view (clears `startDate`/`dependencies`/`actualEndDate`, and the `changelogCache` in Epic Timeline mode so status-driven dates can be re-derived) while keeping developer placeholders and milestones intact. Distinct from the existing destructive "Clear" (which wipes the whole plan). |
| v2.45.0 | Corrected the status-history end marker from "Ready for Deployment" to **"In Review"** (`IN_REVIEW_STATUS`) — many tickets never pass through Ready for Deployment, so their actual end date was never set. Added `IGNORED_STATUSES` ('known issue', 'removed') — skipped entirely by dev auto-assignment, status-history placement, and Auto-schedule. Rewrote Epic Timeline's Auto-schedule: (1) no longer skips unassigned issues — everything not-started now gets placed even with no dev; (2) stories that have subtasks no longer compete in their own assignee's queue (which was double-booking devs and causing the reported overlaps) — a container story's date is now just derived from its earliest-scheduled subtask. |
| v2.46.0 | Epic Timeline's Auto-schedule now resolves a subtask's dev placeholder itself (own Jira assignee, or the parent story's as a lighter-colored `borrowedFromParent` fallback) *before* chaining, instead of only relying on the separate passive auto-assign effect having already run — so a subtask with no assignee of its own still joins its parent's assignee queue and gets a proper dependency link, rather than sitting unscheduled or unchained at `planStart`. |
| v2.47.0 | **Root-caused and fixed the Auto-schedule "nothing appears" blackout**: `planStart` was used raw as the scheduling baseline, but `buildWorkingDays` skips weekends — if `planStart` landed on a Sat/Sun, every freshly-scheduled issue collided on that one invalid date and zero bars rendered (confirmed via simulation). Added `snapToWorkingDay()` (planning.js) and applied it at `planStart`'s init, its date-picker `onChange`, and defensively at every `workingDays.indexOf(...)` lookup (`getBarProps`, `getEpicSummaryBarProps`). Also: unassigned issues in Epic Timeline's Auto-schedule now advance a separate shared `unassignedCursor` instead of colliding with assigned issues on `planStart`; `autoScheduleAll` seeds from raw `plan.issues` instead of the cascaded `computedPlan.issues`; the story-as-container derivation now clears `dependencies` so `cascadePlan` can't silently override its derived date. **Missing-estimate banner**: `roughMap`/`missingEstMap` now use a new `epicScopedHierarchy` memo that narrows `epics`/`storiesByEpic`/`subtasksByStory` to just the focused epic in Epic Timeline mode (was previously computed over the whole version — hence "173 issues" while looking at one epic); `buildMissingEstMap`'s epic-level rollup now only flags an epic when its own derived total (`roughMap[epic.key]`) is unusable, not merely because some unrelated child story is flagged — matching the already-correct story-level rule ("a parent with children doesn't need its own estimate"). |
| v2.48.0 | **Found a second, likely primary cause of "Auto-schedule does nothing" + "can't scroll to past dates"**: `workingDays` only ever extended *forward* from `planStart` — any issue dated before it (e.g. real Jira status-history dates for work that already started) fell completely outside the array, so `getBarProps` silently returned `null` for it — invisible AND unscrollable-to. Added a `windowStart` memo that scans `computedPlan.issues` for the earliest known `startDate` and extends `workingDays` backward to cover it (via the existing `workingDaysBetween` helper), so historical dates are now visible and reachable by scrolling left. Added an auto-scroll-to-today effect (keyed on `selectedPlanId`/`focusEpicKey`) so the view still opens centered on "today" instead of the new, potentially much-earlier `workingDays[0]`. Also added temporary `console.log('[autoScheduleAll] ...')` debug output (candidate counts, per-leaf resolved dates, and whether each is inside the visible window) — check the browser DevTools console (F12) when clicking Auto-schedule to see exactly what it did; remove once the root cause is fully confirmed fixed. |
| v2.49.0 | **Consolidated scheduling so "Clear all → Auto-schedule" is a single deterministic action**: previously, already-started issues' real dates were only ever restored by a separate background `useEffect` racing against `changelogCache`/`plan.issues` changes — after Clear all wipes that cache, Auto-schedule (which only ever handled not-started issues) appeared to "do nothing" while the real repopulation silently happened moments later via the effect. `autoScheduleAll` is now `async`: it still chains not-started leaves synchronously, then directly resolves any "already started" leaf lacking a date (from `changelogCache` if already resolved, else a fresh `getIssueChangelog` fetch via `Promise.all`) *before* the container-story derivation and final `updatePlan`, so one click both schedules and restores history. The button shows "Scheduling…" and disables itself while awaiting (`autoScheduling` state). Also: Auto-schedule now auto-expands every story that has subtasks (`setExpandedStories`) so freshly-scheduled subtasks and their dependency arrows are actually visible without manual clicking — dependency arrows require BOTH endpoints to be present in `sortedRows`, which excludes subtasks of collapsed stories. `clearAllScheduling` now also seeds from raw `plan.issues` instead of `computedPlan.issues`, matching the same fix already applied to `autoScheduleAll`. **Auto zoom-to-fit**: `DAY_WIDTH` (renamed base constant to `BASE_DAY_WIDTH`) is now a computed value — a `ResizeObserver` measures the timeline panel, a `scheduledIndexRange` memo finds the day-index span actually covered by placed/scheduled rows, and day width shrinks (down to `MIN_DAY_WIDTH`) so that whole span fits without horizontal scrolling; never grows past the 50px default. `getEpicSummaryBarProps` now takes `dayWidth` as an explicit parameter since it's a module-level function that can't see the component-local computed value. |
| v2.50.0 | Epic Timeline: dragging/clicking a **started** issue (not To Do/Blocked/Reopened, not ignored) onto the timeline now snaps it to its REAL Jira dates (first In Progress transition → first In Review transition, via changelog fetch/cache in `placeOnTimeline`) instead of honoring the drop position — the drop just triggers placement. New derived **story-level dependency arrows**: when a subtask in story B depends on a subtask in story A, a dashed purple arrow is also drawn between the parent story bars (deduped per story pair, non-interactive), so chains stay visible at parent level and with subtasks collapsed. |
| v2.51.0 | **Epic Timeline: a parent story now ALWAYS spans exactly its subtasks** — a new module-level `computeChildSpan()` helper computes the min-start/max-end across a set of child issues; `getBarProps()` uses it for any story with subtasks (returning `bar.isContainer = true`, rendered as a non-draggable dashed-purple summary bar, no × button, instead of the story's own independent startDate/history), and `getEpicSummaryBarProps()` now also uses it per-story (so the epic bar starts when the first task started, not from a story's own possibly-later date). Refined the derived parent-level dependency arrows to skip drawing when both parent stories are already expanded (the real subtask-level arrow already shows the chain there, avoiding overlapping/confusing double arrows). Locked (real-history) bars now render a **bold 4px border** instead of 2px. |
| v2.52.0 | New **🐛 Debug panel** (Epic Timeline mode, toolbar toggle) — a raw data table of every story+subtask under the focused epic (`debugRows` memo): key, type, parent, status, Jira assignee, assigned dev(s), borrowed-from-parent flag, computed start/end date, actual-history flag, rough hours, dependencies. "⎘ Copy table" copies it as TSV to the clipboard for pasting into chat. Container bars (story spanning subtasks) now use the assignee's color as background (matching leaf bars) instead of a transparent/dashed-purple fill — dashed border kept only as the "this is derived" cue. |
| v2.53.0 | **Two root causes found via the debug table's real exported data**: (1) this Jira instance's "To Do" status is literally named `"ToDo"` (no space) — every status comparison did `.trim().toLowerCase()` only, so `'todo' !== 'to do'` and those issues were silently misclassified as "already started," found no real history (they never started), and got no date at all. Added `normalizeStatusName()` (strips ALL whitespace too) and switched every status comparison in the file to use it. (2) Issues that already had a `startDate` (even stale/wrong ones from before the real-dates fix, e.g. TMT0-41693's fabricated chain) were never re-verified against real Jira history, because every skip-guard checked "does it have *a* startDate" instead of "was this date *confirmed* from history." Added an explicit `entry.historyResolved` flag, set only when a fetch+apply completes for an already-started issue; all three resolution paths (`placeOnTimeline`, the background status-history effect, and `autoScheduleAll`) now key their skip-guard on this flag instead of on `startDate` presence, so any old/stale/manually-set date on a started issue gets corrected the next time it's touched. `clearAllScheduling` also resets the flag. Debug table gained a "Resolved?" column to verify this directly. |
| v2.54.0 | Fixed Auto-schedule's `nextAvail` dev-availability tracking starting empty every run — it only ever accounted for issues scheduled *within that same pass*, never issues already placed (including locked/real-history ones from a prior run), so a not-started leaf could get scheduled right on top of the same dev's already-fixed commitment (e.g. TMT0-40345 landing exactly on TMT0-41789's real DONE dates for the same assignee). `autoScheduleAll`'s epic branch now pre-seeds `nextAvail` from every already-placed leaf in the epic (locked or not) before scheduling anything new. Scoped to within the focused epic — cross-epic overlaps (same dev double-booked across two different epics) aren't covered, since `roughMap` is now epic-scoped and duration data for other epics' issues isn't available here. Existing already-scheduled bad pairs (like the TMT0-40345/41789 overlap) need one Clear all → Auto-schedule cycle to actually resolve, since already-placed not-started issues are otherwise left alone by design. |
| v2.55.0 | Found the v2.54 fix didn't actually prevent the reported overlap: the `nextAvail` pre-seed ran *before* the async real-history resolution block, so right after "Clear all" the soon-to-be-resolved locked issues had no `startDate` yet at pre-seed time and were invisible. Reordered `autoScheduleAllInner` so already-started issues are resolved against real Jira history FIRST, then `nextAvail` is pre-seeded, then not-started items are scheduled — so every dev's real commitments are known before anything new gets placed around them. Also fixed `detectConflicts` (planning.js): it never looked at `entry.actualEndDate`, so locked issues got an estimate-based end date instead of their real one, and it compared a container story's own vestigial stored dates against its own children (always "overlapping" by definition — a parent spans its kids). Added `opts.excludeKeys` (container story keys, now excluded in Epic Timeline mode) and `opts.skipLockedPairs` (drops a conflict where both sides are already real/immutable Jira-history dates — nothing about scheduling can fix two historical facts that already overlapped). |
| v2.56.0 | New `PLACEHOLDER_COLORS` palette (useVersionPlan.js) — 12 deliberately distinct hues with only one orange (old palette had `#FF5630` red-orange *and* `#FF991F` orange, easily confused). New `recolorPlaceholders()` action + "🎨 Fix colors" button reassigns every existing dev's color from the current palette in order, so plans created before this change get fixed too (colors are stored per-placeholder, so the palette change alone doesn't retroactively fix already-created devs). New click-to-focus: clicking a developer chip now toggles `focusDevId` and filters `rows` down to just that dev's rows (`filteredRows` memo — keeps epic rows for context, and a story row if any of its subtasks match even if the story itself doesn't) instead of opening rename; rename moved to a small "✎" icon next to the chip. `sortedRows` and all SVG-layer height/position math (today line, milestones, code-freeze overlay) now key off `filteredRows`/`sortedRows.length` instead of the unfiltered `rows`. |
| v2.57.0 | New "⬇ Export HTML" button (Epic Timeline mode) — `exportTimelineHtml()` builds a self-contained `.html` file (summary cards, a simple CSS-based Gantt scaled to the actual scheduled date range rather than the full padded `workingDays` window, milestones, critical path, team utilization bars, and the full debug table) and triggers a browser download via a `Blob`/`URL.createObjectURL` + programmatic `<a download>` click — no server round-trip. Reuses the existing module-level `computeProjectSpan`/`computeCriticalPath`/`computeDevUtilization`/`workingDaysBetween` helpers (same ones `DeliveryReport` uses) so the numbers match what's shown on-screen. New `escapeHtml()` helper guards against summary/label text breaking the generated markup. |
| v2.58.0 | Polished the HTML export: gradient banner header, card-hover elevation, zebra-striped debug table, week-tick date ruler above the Gantt. Story rows with subtasks are now native `<details>`/`<summary>` — collapsible with zero JS (works the same after the file is saved/reopened anywhere). Each bar's own text now shows `KEY · start → end` truncated via CSS `text-overflow:ellipsis`; the native `title` tooltip always carries the full key + summary + dates, so a too-narrow bar still reveals everything on hover with no JS measurement needed. |
| v2.59.0 | **Added the dependency arrows the export was missing** — each row now carries `data-row-key`/`data-deps` attributes, and a small embedded `<script>` (vanilla JS, since this is a plain downloaded HTML file, not the React app) draws a purple dashed SVG path from each dependency's bar to its dependent's bar via `getBoundingClientRect()`, re-running on every `<details>` `toggle` event (and on load/resize) so arrows stay correct as stories collapse/expand — a row hidden inside a collapsed story is skipped via an `isVisible()` walk up the `closest('details')` chain. Also added faint background gridlines (pure CSS, aligned to the same week ticks as the ruler) behind the bars. **Compacted** the whole layout: row height 36px→26px, bar height 22px→18px, label column 340px→280px, smaller fonts/paddings/margins throughout. |
| v2.91.0 / v2.1.0 (prod) | **Fixed: "I changed the logo but I don't see it reflected on my site."** There were two separate icons in play and only one was wired up: the Marketplace-listing icon (`marketplace/icon.svg`, uploaded in the Developer Console) only ever appears on the Atlassian Marketplace page and install dialogs — it never shows inside Jira itself. The in-app nav icon next to "K1-Planner" in Jira's sidebar is controlled entirely by `manifest.yml`, which never had an `icon` field set, so Jira was showing its generic fallback regardless of what got uploaded to the listing. Added `static/app/public/icon.svg` (bundled into the build output automatically by CRA) and `icon: icon.svg` on the `jira:globalPage` module — deployed to both `development` and `production`. |
| v2.90.0 | **Fixed: "I cannot click the lock, there is no lock to click"** — reported right after dropping TMT0-40883, which has its own Jira Due date and gets locked directly by `placeOnTimeline` (sets `entry.jiraLocked` on the epic's own plan entry). The Draft-mode epic bar's render code only ever checked `bar.jiraLocked` — a DIFFERENT, derived flag `getBarProps` sets only for the unrelated "locked because a child story has a Jira date" case, where the epic's own entry carries no lock flag at all. A directly-locked epic (own Jira date, no locked children) fell through `getBarProps`'s plain `entry.actualEndDate` branch, which returns no `jiraLocked` field — so the bar silently rendered as a normal draggable, developer-colored bar with no 🔒, exactly as reported. Now computes `_lockedByChildren` (`bar.jiraLocked`) and `_lockedDirectly` (`entry.jiraLocked`, only when not already locked-by-children) separately, combines them into `_isLocked` for styling/drag-disable/border, and renders the correct 🔒 with the matching handler for each case — `unlockAllChildren` for locked-by-children, `unlockJiraDates` for a direct lock. |
| v2.89.0 | **New "↻ Refresh from Jira" button** (main toolbar, next to Auto-schedule) — epics/stories were only ever fetched once when the page loaded (`useEpicHierarchy`'s effect ran only on project change), so an epic added to or removed from the version in Jira afterward needed a full page reload to show up, and there was no way to prune ones that got untagged. `useEpicHierarchy` now exposes `refetch`, which re-pulls the whole hierarchy; the button calls it and reports what changed (`+N new (KEYS)` / `−N no longer in version (KEYS)`) as a transient toolbar note, diffed against the version-scoped epic list from just before the refresh (not the hook's raw whole-project fetch, which wouldn't catch an epic just being untagged from this version). **Also fixed: "settings aren't being saved, I have to reconfigure every reload."** `estSource`, `bugFixPct`, `bufferDays`, `autoDepByDev`, `codeFreezeDays`, and `stabilizationDays` were plain component state initialized to hardcoded defaults and never persisted anywhere — every reload or plan switch silently reset them. They're now read from and written into `plan.settings`, riding the existing 1.5s autosave. A plan switch loads its own saved settings back exactly once (guarded by `settingsSyncedForPlanRef`), so a user's in-progress edit can't get clobbered by a stale plan reference. |
| v2.88.0 | **Fixed: "the summary is not correct" — `missingEstimateIssueCount` (and the toolbar's missing-estimate badge) was wildly inflated (1260 flagged issues in a plan with only 118 issues total).** Root cause, found by analyzing the new JSON export: `useEpicHierarchy.js` correctly filters `epics` and `storiesByEpic` down to the selected version, but returned `subtasksByStory` completely UNFILTERED — it's fetched once for every story across the ENTIRE project (all epics, all versions) and was never scoped afterward. Every place that flattens `subtasksByStory` (the missing-estimate map, estimate-coverage %, rough-estimate rollups) was silently counting subtasks belonging to stories from other versions entirely. Fixed by building `filteredSubtasksByStory`, keyed to only the story keys that survive the existing version filter, and returning that instead. **Also investigated the "developers have less than full capacity but have epics assigned" report**: in the exported plan ("final 3.1.1"), all 10 developers show `capacityPct: 100` — nobody is currently below full capacity in this specific plan. If capacity was set below 100% for someone, it isn't present in this plan/version's data; worth confirming which plan that was set in. |
| v2.87.0 | **New "⬇ Export JSON" button** (Tools panel, works in every mode, unlike the Epic-Timeline-only HTML export) — downloads a complete structured dump of the current plan: every epic/story (+ subtasks in Epic Timeline mode) with dates, assignees, total/remaining estimate hours, QA hours, dependencies, and Jira-lock state; every developer with their capacity %, assigned-hours, a capacity-adjusted utilization %, and assigned-issue count; project span, critical path, conflicts, missing-estimate count, milestones, sprints, and the active plan settings. Built from `epics`/`storiesByEpic`/`subtasksByStory` directly (not the UI's `rows`), so it's complete regardless of what happens to be expanded/scrolled into view on screen — not a screenshot of the current view. Same Blob+`<a download>` pattern as the existing HTML export. Intended for pasting into a conversation to debug a number that looks wrong (a capacity/utilization mismatch was reported alongside this request) or as raw material for a dashboard built from it. **On the "refresh loses my progress" worry raised alongside this**: the app already autosaves 1.5s after any edit (Forge-storage-backed, not just browser-local) — the toolbar's save-status chip cycles `● unsaved → ↻ saving… → ✓ saved`; refreshing is safe once it reads "✓ saved", but a refresh within that ~1.5s debounce window, or right after a Forge storage write actually fails (chip would read "✗ save error"), can still lose the most recent edit — no code change made here, flagging it since the concern is legitimate for that narrow window. |
| v2.86.0 | **Fixed: sprints were fetching fine (v2.85.0) but their colored header bands still weren't visible — "I don't see the sprint colors."** CSS stacking issue: the sprint bands (`position: absolute`, no z-index) and the date-header day cells (`position: relative`, rendered LATER in DOM order, also no z-index) both land in the same default stacking layer. Per CSS paint-order rules, same-layer positioned siblings paint in DOM order regardless of which one is visually "behind" — so the day cells, being later in the JSX, painted over the sprint bands even though each day cell's own background is `transparent`. Gave the sprint band an explicit `zIndex: 1`, which promotes it to a layer that always paints above z-index:auto siblings regardless of DOM order — matches why the milestone labels (already `zIndex: 20+`) never had this problem. |
| v2.85.0 | **Fixed: "we have many sprints already defined, but I still don't see them."** Root cause: `getSprintsForBoard` (bridge.js) queried Jira's Agile API with `state=active,future` only — every CLOSED sprint was silently excluded, and a real, actively-used project's sprints are mostly closed/historical, with at most one active and a couple future ones. Removed the state filter entirely; fetching more is safe since the Gantt already only renders whichever sprints' dates fall inside the currently-visible `workingDays` window; an off-screen closed sprint from years ago costs nothing extra to have fetched. Also hardened the whole fetch chain, since two silent-failure gaps compounded the "nothing shows and no one can tell why" problem: `getSprintsForBoard` had no try/catch of its own, so ONE board without sprint capability (e.g. a plain kanban board mixed in with a Scrum board) would reject and, via `Promise.all` in `useSprints`, wipe out every OTHER board's sprints for the whole project — it now catches its own errors and returns `[]`, matching the pattern `getBoardsForProject` already used. Both now `console.warn` on failure instead of swallowing it silently, and the component didn't even read the hook's `error` state before this — it now does, logging a warning if the fetch as a whole fails. |
| v2.84.0 | **Fixed: milestone labels invisible and unclickable, reported as "I see the dot but no label and can't edit it."** Root cause: v2.82.0's sprint bands added an opaque background to the BOTTOM of the sticky date-header (`zIndex: 10`), which sits at the exact same y-range (`HEADER_H - 16` to `HEADER_H - 2`) as the milestone's SVG label pill, rendered in a separate, lower-stacked SVG overlay below the header in DOM order — so the sprint band now painted over the milestone label, hiding it and blocking its clicks. Only the OLD simpler day-cell dot marker (no label, no click handler — a `hasMilestone` check + 6px circle, unrelated to the SVG pill) remained visible, which is exactly what was reported. Fixed by moving the milestone label itself into the sticky header's TOP strip (as a plain positioned `<div>`, same technique as the sprint bands, which now own the bottom strip) — properly clickable since it's in the same stacking context as everything else in the header, not fighting an SVG z-index issue. The SVG overlay keeps only the dashed vertical line through the rows, itself still independently clickable to edit. Removed the now-redundant/misleading day-cell dot entirely. **Also fixed same-day milestone overlap** (multiple milestones on one date rendered exactly on top of each other, invisible except the topmost): grouped milestones by date and horizontally stagger each one within a group by its index × a fraction of its own pill width, so they fan out instead of fully overlapping; each stays independently clickable with a tooltip noting how many share that date. |
| v2.83.0 | **Draft mode: an epic with locked child stories is now shown locked too, spanning their combined date range.** Since v2.80.0 put child stories in Draft mode's own timeline (draggable, lockable via v2.82.0's Jira due-date logic), dragging a story with a Jira Due date locks it — but the epic bar was still computed independently from the epic's own estimate, hiding the fact that part of it is already committed. `getBarProps` now checks, before its normal per-epic calculation, whether ANY of an epic's child stories are `jiraLocked`; if so it returns a bar spanning the earliest locked child's start to the latest locked child's end (grey fill, 🔒), **unconditionally overriding the epic's own stored placement** the same way an Epic Timeline container overrides its subtasks' parent. Since the epic has no date of its own to unlock in this state, its 🔒 performs a bulk `unlockAllChildren()` across every locked child instead. Both the drag handle and the "×" remove button are disabled while locked-by-children — the bar's span comes entirely from the children, so acting on the epic's own (unused) entry would silently do nothing, which reads as a broken interaction rather than a no-op; both point users at the 🔒 instead. |
| v2.81.0 | **Hotfix: `remainingH is not defined` broke every Draft-mode Gantt bar.** My own mistake from v2.78.0 — the Draft-mode epic bar's tooltip referenced `remainingH`, a variable that only exists in the LEFT TABLE's row-rendering loop (a separate closure from the right-hand Gantt bar loop). Fixed by reading `remainingEstMap[row.key]` directly at the point of use instead. **Important caveat surfaced while fixing this**: `src/__tests__/components.test.js` never renders `VersionPlanningView` at all (only `TeamView`/`GlobalSearch`/`TutorialOverlay`) — so "all 33 tests pass" has never meant anything for bugs in this file, including this one and a second one caught before deploy (see v2.82.0). CRA's build also doesn't run `no-undef` linting on this file. Until real coverage exists here, every VersionPlanningView change needs a manual scope check of new identifiers, not just a green build/test. |
| v2.82.0 | Due-date locking + more visible sprint markers, found while fixing v2.81.0's live break. (1) **Dropping an issue with a committed Jira Due date now honors it and locks the bar** (grey fill, 🔒, `cursor: not-allowed`) instead of using the drop position — clicking the 🔒 unlocks it (`unlockJiraDates`, keeps the current start, drops the fixed end, sets `jiraUnlocked` so it won't re-lock on the next drop). With a Start-date custom field value too, both ends are real; with only a Due date, the start is derived by counting the issue's estimated duration backward from it (new `subWorkingDays()` in planning.js) so the work finishes exactly on commitment — per explicit user refinement mid-build ("use the due date and count backwards... if they don't have start date"). `getSubtasksForStories` now also requests the start-date custom field (epics/stories already did; subtasks were missing it). New `issueByKey` memo and `jiraDateWindow(issue, startFieldId, durationDays)` helper. (2) **Sprint bands in the date header** — a small colored, labeled strip along the header's bottom edge spanning each sprint's date range (alternating two colors by index, clipped to the visible window), complementing the existing thin dotted boundary lines in the Gantt body from v2.71.0, which were easy to miss while scrolling. **Caught and fixed two more of the same undefined-variable-in-wrong-scope bug _before_ this deploy** (not after, this time): a `remainingH` leak into the SAME Draft-mode bar tooltip fixed in v2.81.0 turned out to have a sibling reference a few lines down that v2.81.0's targeted fix didn't cover, and a fresh `subWorkingDays` reference in the new due-date code was never added to the `planning.js` import line. Both found via a manual reference-count sweep (`grep -c` per new identifier, expecting ≥2: one definition + one use) since the test suite can't catch them — see v2.81.0's note. |
| v2.80.0 | **Draft mode now lists child stories under expandable epics, same as Final.** The `rows` memo's Draft branch (previously `epics.map(...)`, a flat epics-only list) was deleted so Draft falls through to the shared epic+children builder — the two modes differ in how they SAVE (Draft keeps many named plans, Final is one committed plan) and in what the epic's bar means, not in row hierarchy. Story rows now also carry `_isStory: true` in both modes. Two knock-on fixes required: (a) `sortedRows` had a Draft fast-path (`[...filteredRows].sort(cmp)`) that sorted flat — with children present that would interleave stories under unrelated epics, so Draft now uses the same epic-grouped sort as Final; (b) Auto-schedule's Draft/Final branch now filters candidates to `row._isEpic` in Draft only — Draft's premise is epic-level scheduling (duration = rough est ÷ devs, with QA/Fix extensions on the epic's own bar), so its child rows are context for reading the epic's numbers, and without the filter expanding an epic would start double-booking its devs against both the epic and its own stories. The expand caret now renders in Draft as well (still only when children exist). Judgment call worth revisiting: child stories in Draft are visible/estimable/assignable and *can* be dragged onto the timeline, but Auto-schedule deliberately ignores them so epic-level planning results are unchanged from before this release. |
| v2.79.0 | Three changes. (1) **Reverse a dependency's direction in place.** Previously the only route was "click the arrow to delete, then redraw it by hand in dependency mode" — which auto-chaining then tends to undo on the next drop. New `reverseDependency(source, target)` rewrites both sides in ONE `updatePlan` call (so `cascadePlan` never sees an intermediate state where the edge exists in both directions) and **refuses when the flip would create a cycle** — it walks the target's remaining dependency chain, ignoring the edge being removed, and bails if it leads back to the source, because `cascadePlan` topologically sorts and a cycle silently drops every issue in it from scheduling (its queue never drains). Exposed two ways on each arrow: a **⇄ handle at the curve's midpoint** (computed at t=0.5 of the cubic; needs `pointerEvents:'auto'` since the whole SVG overlay is `'none'` after v2.65.0) and **Shift+click on the line** — plain click still removes, as before. Result feedback lands in a new self-clearing `depNote` chip on the panel-toggle row (green for success, red for a refused cycle). (2) **Fixed: Final mode showed an expand arrow but no children.** `useEpicHierarchy` listed an epic if the EPIC carried the selected version, but kept a story only if the STORY carried it — so an epic tagged to the version whose stories are untagged rendered a caret over zero children (and lost those hours from every rollup). An **untagged story now inherits its epic's version**; a story explicitly tagged to a DIFFERENT version is still excluded, so work already shipped in an earlier version can't leak into this plan's totals. Carets in both Final and Epic Timeline modes now only render when children actually exist (a dim `·` with an explanatory tooltip otherwise), so an arrow never opens to nothing again. (3) **New "Rough" column** showing the Rough Estimation field's raw Jira value (`rawRoughMap`, built via `ownEstHours(issue, 'rough', …)` merged with pending inline edits) — deliberately independent of the Estimate-source toggle and of every rollup/inheritance fallback, so it can be compared against whatever Est/Total Est currently reports. Sortable (`getSortValue` gained a `rough` case and a `rawRoughMap` param). |
| v2.78.0 | **Fixed: an In-Review epic with 0 remaining hours got a bare 1-day bar and no QA/Fix tail.** v2.75.0 based the QA/bug-fix buffer on a rollup of the epic's not-yet-DONE child *stories* — but an epic can have every story already signed off as Done while the EPIC itself still sits in In Review / Ready for Testing / Ready for Deployment, i.e. it is in QA right now. That rollup returns 0, which zeroed the rework budget entirely. Rework is proportional to the code that was WRITTEN, not to the zero dev hours remaining, so new `buildBugFixBaseMap()` wraps `buildEpicRollupMap(..., isDoneStatus)` and falls back to the epic's own total whenever the epic itself hasn't reached Done (only a genuinely Done or ignored epic gets no buffer). Also confirmed the "QA d" column is manual-only and its days↔hours conversion is correct (`qaHours = days * HOURS_PER_DAY`), so automatic QA days were never expected from status alone — the missing block was the bug-fix one. Bar tooltip now explains the 1-day case: "no dev hours left (all stories dev-done); shown as a 1-day marker, with QA/Fix time still reserved after it", since `calcDays` floors at 1 and a zero-length bar would be invisible. |
| v2.77.0 | Team-roster fixes + developer auto-chaining. (1) **Fixed "deleting a developer adds them back and duplicates them."** Two independent causes: (a) the auto-assign effects re-derive placeholders from Jira issue assignees on every plan change, so a deleted dev was resurrected on the next render — `removePlaceholder` now records the dev's `accountId` in a new `plan.dismissedAccountIds` list and `ensurePlaceholderForAssignee` returns null for dismissed accounts, so deletions stick; (b) `ensurePlaceholderForAssignee` only ever matched placeholders by its deterministic `acc_<accountId>` id, so a dev added manually by name (a `plan_*` id, no accountId) was never recognised as the same person and got shadowed by a second chip — it now also matches an accountId-less placeholder by name (case-insensitive) and ADOPTS it by attaching the accountId, so one human can only appear once. Added `resetDismissedAssignees()` behind an "↺ Re-detect (n)" button (only rendered when something is dismissed) as the escape hatch. (2) **New "🧹 Keep only assigned" button** (`pruneUnusedPlaceholders()`) drops every dev not assigned to at least one issue and dismisses their accountIds so they don't come straight back; each Team-panel chip now also shows its assigned-issue count (new `assignedCountByDev` memo) so it's obvious who is carrying work before pruning. (3) **Developer circles now show 2-letter initials** instead of a bare color dot — new `devInitials()` helper ("Sharon Cohen" → SC, single-word names → first two chars), applied both to the Team panel chips (`Chip` gained an optional `initials` prop rendering an 18px avatar) and to the left table's per-row assigned dots (17px, 7px text). (4) **New Draft-mode "Auto-chain epics sharing a developer" setting** (`autoDepByDev`, default on, in the Settings panel): dropping an epic now calls `autoDepOnDrop()`, which picks the latest-ENDING already-placed epic that shares a developer and starts at/before the drop point, and sets it as the dropped epic's dependency — so the drop position still chooses where in that dev's queue the work lands, while `cascadePlan` owns the exact date (it always recomputes startDate from dependencies, so the bar snaps to just after its predecessor rather than sitting exactly where the cursor was released). Off by unchecking, since that snap is deliberate but surprising if unexpected. |
| v2.76.0 | **Fixed the epic-level twin of the v2.74.0 rollup bug: an epic estimated as one lump reported 0h Remaining.** Reported via TMT0-38353 — Total Est 210h, Remaining 0h, coverage 0%, which is self-contradictory. The epic carried 210h in its own Rough Estimation field while none of its 11 stories carried one, so the story rollup summed to 0 and *overwrote* the epic's real total. Dangerous, not just cosmetic: `calcDays(0)` floors at 1, so a 210h epic would schedule as a single day. v2.74.0 fixed exactly this shape at story→subtask level but left epic→story untouched. Generalized `distributeParentEstToSubtasks` into `distributeParentEst(parentHours, children, childHours, map)` and now apply it at BOTH levels — epic→stories first (each unestimated story inherits an even share of the epic's unaccounted hours), then story→subtasks using the story's *effective* hours so the share reaches the leaves. Both children-mode rollups now also floor at what the parent already has (`Math.max(sum, map[parent])`) so a rollup can never destroy a real estimate. New `storyRealHours()` helper defines "does this story genuinely carry an estimate" (own value, else sum of its subtasks' own values, never an inherited share). **The coverage badge now uses `storyRealHours` instead of the post-inheritance `roughMap`** — otherwise inheritance would have made every such epic report 100% coverage and hidden the very problem the badge exists to flag. Badge tooltip rewritten to explain the situation rather than read as a contradiction: at 0% it now says the epic's total is being spread evenly across its stories for scheduling and to estimate them individually for an accurate plan. |
| v2.75.0 | Two changes. (1) **Fixed: dev-done epics lost their QA/bug-fix buffer entirely.** v2.73.0 routed *every* Draft-mode calculation through `schedMap` (remaining dev hours, which excludes all four `DEV_DONE_STATUSES`), including `calcQaBugFixDays` — so an epic whose children sat in In Review / Ready for Testing / Ready for Deployment had 0 hours left to multiply by `bugFixPct` and got no rework budget at all, exactly the epics most likely to still generate bugs. Duration and buffer are now sized from different bases: new `isDoneStatus()` (only `done`, deliberately narrower than `DEV_DONE_STATUSES`) plus a generalized `buildEpicRollupMap(epics, storiesByEpic, roughMap, shouldExclude)` — which replaced `buildRemainingEstMap` — produce **`remainingEstMap`** (excludes dev-done → drives bar duration) and **`bugFixBaseMap`** (excludes only Done → drives the QA/bug-fix buffer). `cascadePlan`/`detectConflicts` gained an optional `opts.bugFixBaseMap` (falling back to `roughMap`, so Epic Timeline/Final behavior is untouched), and all four Draft-mode `calcQaBugFixDays` call sites — cascade chaining, conflict end-dates, `lastEffectiveEnd` (code-freeze/stabilization), the epic bar's QA/Fix extension blocks, and dependency-arrow origins — now pass `qaBaseMap`. Net effect: only genuinely Done work is exempt from the buffer; every other status still reserves rework time. (2) **New per-epic estimate-coverage badge.** `buildEstCoverageMap()` counts how many of an epic's non-ignored child stories have a usable estimate (`roughMap[key] > 0`, so an own estimate or a leaf-children rollup both qualify) and renders a small `%` badge in the epic row's Summary cell whenever coverage is below 100% — amber under 100%, red under 50%, tooltip spelling out "only N of M stories are estimated". An epic with 10 stories and 1 estimate now visibly reads `10%` instead of presenting a total that silently represents a tenth of its scope. |
| v2.74.0 | **Fixed "epic shows no estimates even though its stories are estimated"** (reported for TMT0-42198). Root cause in `buildRoughMap`'s `children` mode: when a story HAS subtasks it summed ONLY the subtasks' Original Estimates, and the fallback to the story's own estimate lived in the `else` branch — reachable only when the story had *zero* subtasks. So the extremely common shape "story estimated, subtasks broken out but left unestimated" summed to 0, which zeroed the story AND (since epics sum their stories) reported the whole epic as unestimated. Rewrote the function around two new helpers: `ownEstHours(issue, mode, fieldId)` (an issue's own estimate in hours — Rough Estimation field in `rough` mode, `timeoriginalestimate/3600` in `children` mode, 0 when absent) and `distributeParentEstToSubtasks()`, which spreads a story's UNACCOUNTED hours (`parentOwn − sum of subtasks that do have estimates`, floored at 0) evenly across only the subtasks lacking a value — so subtasks now sum to `max(parentOwn, knownSum)`, honoring the parent's number without double-counting or overwriting estimates already entered. Story rollup is now `Math.max(subSum, ownEst)` so a story never reports less than what it was estimated at. Inheritance applies in BOTH modes (a subtask with no Rough Estimation now inherits from its story too, so it gets a real duration in Epic Timeline mode); the story-from-subtasks rollup stays `children`-mode-only, preserving `rough` mode's "each level uses its own field" semantics. Also simplified `buildMissingEstMap` to a single rule — flagged ⇔ `roughMap[key] <= 0`, i.e. "we genuinely cannot compute a duration for this row" — keyed off the already-built map instead of re-reading raw fields, so every fallback above automatically counts as estimated and rows we CAN schedule are no longer reported as missing estimates (its now-unused `mode`/`fieldId` params were dropped). |
| v2.73.0 | **Draft mode: epics now schedule by REMAINING estimate, not total.** New `DEV_DONE_STATUSES` (`inreview`, `readyfortesting`, `readyfordeployment`, `done`) + `isDevDoneStatus()` define "dev work finished"; new `buildRemainingEstMap(epics, storiesByEpic, roughMap)` computes each epic's remaining hours as the sum of its child stories' estimates EXCLUDING dev-done and ignored-status children (non-epic keys pass through untouched, so it's a drop-in substitute for `roughMap`). Introduced a single `schedMap` indirection (`planningMode === 'draft' ? remainingEstMap : roughMap`) and swapped it in at every scheduling/duration site — `cascadePlan`, `detectConflicts`, `computeCriticalPath`, `getBarProps`, the auto-zoom span, `lastEffectiveEnd` (code-freeze/stabilization), Draft/Final `autoScheduleAllInner`, the Draft-mode QA/bug-fix bar extensions and dependency-arrow origins, `handleSave`'s Jira due-date writes, the Summary panel, and DeliveryReport — while the "Total Est" column, its inline editor, and `missingEstMap` keep reading the true `roughMap`. So an epic with 30h total whose children are mostly dev-done now occupies only its remaining hours on the timeline. **New "Remaining" column** (Draft mode only, sortable, resizable, green when less than total) sits next to a relabeled "Total Est"; `getSortValue` now takes both maps so `est` sorts by total while `remaining`/`days` sort by what's actually scheduled. **Epic dev roster auto-derives from the union** of the epic's own Jira assignee and all its child stories' assignees (new Draft-mode effect, mirroring the existing Epic Timeline one — only fills epics with nothing assigned yet, so manual assignment is never overridden), so an epic's combined team capacity drives its duration. **Row status tints** (left table only, never the Gantt): very light red `#FFF5F4` for Validation, very light green `#F3FCF7` for any dev-done status — layered under the existing deps-mode/scheduled highlights so those still win. |
| v2.72.0 | Two fixes: (1) **Estimate source toggle (Rough Est / Children Sum) now shows in Draft mode too** — it was gated to `planningMode === 'final' \|\| 'epic'` only, an oversight from an earlier redesign, even though `buildRoughMap`'s "children" mode has always summed each epic's child stories' original estimates (Draft mode schedules at the epic level, so this was a real, usable option that was just hidden). Now shown unconditionally in the Settings panel for all three modes. (2) **Fixed the left table's "Assigned" column visually overlapping rows once a team has more than ~4-5 developers** — it was rendering a dot for literally EVERY placeholder in the whole roster on every row (colored if assigned, hollow if not), so an 11-developer team meant 11 dots per row regardless of how many were actually assigned to that issue; at the column's default 90px width those wrapped to 2-3 lines, and since the row height is fixed (kept in sync with the Gantt panel's row height on the other side), the overflow bled visually into neighboring rows. Now only the devs actually assigned to that row render as dots (click to unassign), plus a small "+" native-select listing the still-unassigned devs to add one — so a big roster no longer bloats every row regardless of that row's real assignment count. |
| v2.71.0 | **Grey dotted sprint start/end lines on the Gantt**, pulled live from Jira's own sprint schedule for the project — not editable here, purely a reference overlay. Reused the already-existing (but previously unused in this view) `useSprints(projectKeys)` hook / `getBoardsForProject`+`getSprintsForBoard` bridge calls (already powering sprint pickers elsewhere in the app, e.g. `EpicHierarchyPanel`) instead of writing a new fetch path. Each sprint contributes two dashed grey (`#97A0AF`) vertical lines drawn at the day-COLUMN EDGES (not the center, unlike the today-line/milestone lines) so they visually bracket the sprint's date range; the start line also carries a small grey sprint-name label. Dates come back from Jira as full ISO datetimes, sliced to `yyyy-MM-dd` and passed through the existing `snapToWorkingDay` before being looked up in `workingDays`, consistent with how every other date on this timeline is handled. Only active/future sprints are fetched (`getSprintsForBoard`'s existing `state=active,future` filter), so past/closed sprints don't clutter the view. |
| v2.70.0 | **Gantt bars now show their Jira status as a border color**, independent of the fill color (which stays the assigned developer's color, or a gradient for multiple devs). New `STATUS_BORDER_COLORS` map + `statusBorderColor()` helper (normalized status name → hex): Done → grey `#97A0AF`; Ready for Deployment / Ready for Test → blue `#0052CC`; Blocked / Monitoring / Validation → red `#DE350B`. Everything else (To Do, In Progress, In Review, …) gets no status border, so the bar's own dev-colored border/outline (conflict red, locked-history green, critical-path orange) shows through unchanged — matching the request that those "normal working" states stay identified by developer color, not a status color. Precedence per bar: a scheduling conflict (red) always wins first, then the status color (3px, deliberately thicker so it reads at a glance), then the existing locked/real-history green border, then default. Applied to both bar-rendering branches that represent a single real issue (Draft-mode epic bars, and the standard Epic-Timeline/Final-mode bar) — left untouched on the container-bar branch (a story-with-subtasks span), since a container can hold subtasks in several different statuses at once and no single color would be accurate there. |
| v2.69.0 | **"Clear all scheduling" now offers to remove the matching due dates from Jira.** Previously it only cleared local schedule state (startDate/dependencies/actualEndDate) and left whatever due dates a prior "Save to Jira" had already written untouched, silently going stale. Clicking it now opens a confirmation dialog (`PlanDialogModal`'s new `clearAllScheduling` type — three buttons: Cancel / Clear only / Clear + remove Jira due dates) instead of clearing immediately, since pushing null due dates to real Jira issues is exactly the kind of external, harder-to-reverse action that should ask first. `clearAllScheduling(removeJiraDueDates)` is now async: it captures which issue keys actually had a `startDate` before clearing, and — only if the user picked "Clear + remove Jira due dates" — calls the existing `updateIssueDueDate(key, null)` (Jira accepts `null` to clear the field) for each of them in parallel, swallowing individual failures so one bad issue doesn't block the rest. |
| v2.68.0 | **Per-developer capacity %** — each placeholder now has a `capacityPct` (default 100), editable in the Team panel (a `%` number input next to each dev chip). A dev at 80% contributes 0.8 "effective devs" toward any issue's daily throughput instead of a full 1 — e.g. one dev at 100% + one at 80% count as 1.8 effective devs, not 2, so `calcDays(hours, effectiveDevs)` naturally produces longer durations without any change to `calcDays`/`calcEndDate` themselves. New `effectiveDevCount(assignedIds, placeholders)` (exported from `planning.js`) computes this sum, defaulting missing/unfound placeholders to 100% for backward compatibility; every dev-count call site that used to do `(entry.assignedPlaceholders || []).length || 1` across both `planning.js` (`cascadePlan`, `detectConflicts`, `findCriticalPath`) and `VersionPlanningView.js` (~15 sites: bar duration, critical path, project span, dev-utilization split, auto-schedule, `placeStoryAndSubtasks`, `handleSave`'s due-date write, the debug table, epic/story summary bars) now goes through it — plain assignment-count checks (sort-by-assigned-count, "does this row have any dev" existence checks, debug log field) were deliberately left alone since they're not duration math. Dev-utilization's hour split across co-assigned devs and its capacity bar's denominator are now capacity-weighted too (a 50% dev takes proportionally less of the shared hours and is judged against a smaller capacity ceiling). Shown in the timeline: a dev's avatar circle on a Gantt bar gets a dashed (vs. solid) border when capacity < 100%, and every capacity-aware tooltip (bar avatars, the left table's assigned-dev dots, the Summary panel's utilization list, the Delivery Report) appends "(N% capacity)" or "(N%)". Also: the version `<select>` (both the pre-version-selection screen and the toolbar) is now naturally/numerically sorted by name via a new `selectableVersions` memo, replacing the previous unsorted API order. |
| v2.67.0 | **Draft vs. Final is now a real save-behavior distinction, not just a view mode.** Per user request: Epic Timeline mode gets its own nested Draft/Final toggle (next to the epic selector, separate from the top-level Draft/Final/Epic Timeline mode buttons). "Final" — whether the top-level mode or Epic Timeline's nested choice — means exactly ONE reserved, auto-created plan for that scope (`Final` for top-level Final mode, `Final — <epicKey>` per epic for Epic Timeline's Final choice): no plan picker, no New/Save-as/Rename/Delete (shown instead as a "🔒 Final plan — saved directly to Jira" badge), and the "Update Jira due dates" checkbox is forced checked and disabled (`isFinalContext` derived value drives all three). "Draft" (top-level Draft mode, or Epic Timeline's nested Draft choice) keeps the existing multi-plan picker and Save-as/Rename/Delete, sharing the same version-wide draft-plan pool either way — but the reserved Final-named plans are filtered out of that list (`draftPlanIndex`) so they can never be renamed or deleted by accident. New-plan default names are now contextual (`{project} · {version} [· {epicKey}] Draft {n}`) instead of generic "Plan 1", "Plan 2" — still manually editable before confirming, per explicit user preference (auto-fills the placeholder/default rather than forcing a fixed name). Implementation: reserved-name constants (`FINAL_PLAN_NAME`, `finalPlanName()`, `isReservedFinalPlanName()`), replaced the old "auto-select first plan" effect with one that auto-creates/selects the right reserved Final plan (guarded by a `creatingFinalRef` to avoid double-creation races) or falls back to the first available draft plan, and a new `epicPlanKind` state for the nested toggle. |
| v2.66.0 | **Version Planning screen decluttered** — per direct user request ("too crowded with summary and filters and alerts and dependency flags... minimum buttons... extra info as buttons that open a right pane"). The top toolbar now shows only the required controls (version/plan selector, +New, planning-mode toggle, epic selector, Maximize, Auto-schedule, Update-due-dates checkbox, Save to Jira) plus one new row of 5 toggle buttons: 📊 Summary, ⚙ Settings, 👥 Team, 🔗 Tools, and (Epic Timeline mode only) 🐛 Debug. Clicking one opens a 340px right-hand slide-out panel (new `SidePanel` component); only one tab is open at a time (`activePanel` state). Everything that used to be a permanent banner or a secondary toolbar row moved into a tab: **Summary** — the collapsible project-span/critical-path/dev-utilization panel plus the missing-estimate warning, the conflict list (with its "Auto-chain" actions), and the Draft-mode caption, all merged into one `SummaryPanelContent`, with a live alert-count badge on the toolbar button so problems are still visible without a banner; **Settings** — Est source toggle, Draft mode's Bug fix/Freeze/Stab inputs (or Epic Timeline's Buffer input), Plan start date, and plan management (Save as/Rename/Delete, previously separate toolbar buttons); **Team** — the developer chip list, add/rename/recolor, and the click-to-focus filter (per-row dev assignment still works via the small colored dots in the table's Assigned column, untouched by this change); **Tools** — the +Dependency mode toggle, +Milestone button, Export HTML, Clear all scheduling, and Clear plan; **Debug** — the raw schedule table + copy-table button. Also removed the `overlapAlert` toast banner and its supporting effect/ref entirely (dead weight once conflicts are visible via the Summary badge) and dropped the now-redundant `showDebugPanel` boolean in favor of `activePanel === 'debug'`. |
| v2.65.0 | **THE actual root cause of the multi-day "can't drag/click anything already on the Gantt" bug** (v2.60/v2.61/v2.64 fixed real but secondary bugs — lock gating, container-bar not draggable, a milestone hit-rect — that never fully resolved it). The whole-grid `<svg>` overlay (today-line + dependency arrows + milestone lines, `VersionPlanningView.js` ~line 2777) is the LAST sibling rendered in the timeline stack, so it visually paints on top of every row's bar in every column. Its root `<svg>` tag itself never had `pointerEvents` set — and unlike pure-SVG-internal shapes, an inline `<svg>` positioned via CSS is hit-tested as a normal box by the browser (`pointer-events: auto` by default), meaning its ENTIRE bounding rectangle (the whole grid, every row/column, painted or not) swallowed every mousedown before it could ever reach a bar `<div>` or day-cell beneath it. Since the SVG is a *sibling* of the bars, not their ancestor, there was no draggable ancestor for the browser to fall back to — no real HTML5 drag ever started, matching the exact symptoms reported: zero console/debug output no matter how deep the tracing went, a native-looking "no-drop" cursor or rubber-band-style ghost box while attempting to drag, and the bar always snapping back on release. This also explains why dragging FROM THE LEFT LIST to place an issue for the first time kept working the whole time: that drop's `onDrop` handler lives on the right timeline's own scrollable container div, an ANCESTOR of the svg, and native `drop`/`dragover` events bubble up through ancestors regardless of which topmost element the pointer is over — so the ancestor-level handler still fired even though the svg sat on top of everything inside it. Root-caused by a dedicated Explore-agent deep read of the entire component plus every effect/state hook (ruled out re-render races, CSS ancestor issues, duplicate handlers, and stray `stopPropagation` calls first). Fixed with one line: `pointerEvents: 'none'` on the root `<svg>`'s style. Its interactive children (the milestone `<g>`, which now explicitly sets `pointerEvents: 'auto'` so its own subtree re-enables hit-testing despite the parent; the dependency-arrow `<path>`s, which already had their own explicit `pointerEvents: 'stroke'`) continue to work unaffected, since CSS lets a descendant re-opt-in to pointer events even under a `none` ancestor. |
| v2.64.0 | **Fixed the real root cause of "once a task is on the timeline, dragging or clicking it does nothing"** (v2.60/v2.61 fixed the lock/container gating, but a separate bug remained). The SVG dependency/milestone overlay layer (rendered once, after ALL row bars, so it visually sits on top of every bar in the Gantt) drew an invisible hit-rectangle at each milestone's date column to make its thin dashed line easier to click — but that rect spanned `y=0` to the FULL height of every row (`HEADER_H + sortedRows.length * ROW_HEIGHT`), not just the milestone label area. Any bar sitting in the same date column as a milestone had this transparent-but-painted rect on top of it, silently swallowing every mousedown before it ever reached the bar's own `onDragStart`/the day-cell's `onClick` — no console error, no React state change, nothing to see except the browser's native "no valid drag source under cursor" cursor and (misleadingly) a native rubber-band-selection box while dragging, which looked like a stuck ghost image. Root-caused via a temporary on-screen debug readout (bypassing the Jira Custom-UI iframe's console-context confusion) that proved `onDragStart` genuinely never fired for already-placed bars, which narrowed it to a stacking/hit-testing problem rather than anything in the placement logic itself. Fixed by shrinking the invisible hit-rect to just the label-pill area near the header (`y = HEADER_H - 20`, height `24`) instead of the full column, and giving the visible dashed line itself `pointer-events: stroke` so its own thin visible line stays clickable without needing a giant invisible hit box behind it. Milestones are still easy to click (near their label); they no longer block anything underneath them. |
| v2.61.0 | **Fixed root cause of "TMT0-41700 shows a 🚫 no-drop cursor, nothing happens when dragged"**: that issue is a story with subtasks, which Epic Timeline mode always renders as a "container" bar (`bar.isContainer`, `getBarProps` line ~1491) whose span is derived from its children via `computeChildSpan` rather than its own stored date. The container-bar JSX block (the `bar?.isContainer` branch) never had `draggable`/`onDragStart` at all — a separate code path from the individual-bar fix in v2.60.0 — so attempting to drag it hit the browser's native "no valid drop target" cursor (`onDragOver`'s `preventDefault` only fires when `window.__versionPlanDrag` is set, which never happened since no `onDragStart` ever set it). Fixed by adding the same `draggable`/`onDragStart`/`onDragEnd` wiring used by the standard bar branch, plus `cursor: 'grab'`, and removing the leftover `pointerEvents: 'none'` (which is now unnecessary — bars in general already only block clicks passing through to the day-cell layer directly under their own width, matching the standard bar's existing behavior). Dropping a container bar routes through the existing `placeOnTimeline` → `placeStoryAndSubtasks(storyKey, day)` path, which reschedules the story and all of its subtasks back-to-back from the new date (each subtask still chains off the previous one's end + buffer), so moving the story now correctly moves its subtasks/dependents together. |
| v2.60.0 | **Locked (real-Jira-history) bars are now manually movable** — reverses the v2.42.0 "locked = not draggable" design per direct user request. All three locked-bar gates (left-table row drag, day-cell click, Gantt bar drag) were removed; tooltips/🔒 titles now read "drag to override" instead of "locked". `placeOnTimeline` was drastically simplified: the old async "snap to real Jira dates on drop" branch (and its now-dead `findEpicIssueRow` helper) is gone, since the background status-history effect already handles that proactively — placement now unconditionally sets `{ startDate, dependencies: [], actualEndDate: undefined, historyResolved: true }`, so a manual drag always wins and can never be silently reverted by that effect or a future Auto-schedule run. Also removed the now-pointless `async` keyword from `placeOnTimeline` (no `await` remained after the branch removal; both call sites already didn't await it). **Fixed a real cascade bug**: `cascadePlan` and `autoScheduleAllInner` computed a dependency's end date with `calcEndDate` (an *estimate* from rough hours) even when the dependency was locked and had a real `entry.actualEndDate` — so moving/resolving a locked source task didn't correctly cascade to its dependents. Both now prefer `de.actualEndDate` over the estimate, matching the same fix already applied to `detectConflicts` in v2.55.0. **New Buffer setting** (Epic Timeline mode only): a "Buffer: [N] d" numeric input in the toolbar (next to where Draft mode shows Bug fix/Freeze/Stab) sets extra working days inserted after a dependency's end before its dependent can start — threaded through `cascadePlan`'s `opts.bufferDays`, `placeStoryAndSubtasks`'s subtask cascade, and `autoScheduleAllInner`'s pre-seed/not-started-scheduling/`nextAvail`/`unassignedCursor` logic (all via `addWorkingDays(end, 1 + bufferDays)` instead of `nextWorkDay(end)`), analogous to Draft mode's existing QA/bug-fix/freeze buffers. |
---

## Tests
```bash
cd static/app && npm test -- --watchAll=false
# 33 tests, all passing
```

---

## Known Limitations / Next Ideas
- Export to PDF (currently only Markdown copy)
- Final mode code freeze / stabilization lines (currently Draft mode only)
- The "⎘ Copy" button uses `navigator.clipboard` — may fail in some Jira iframe contexts; no fallback yet
- `workingDays` array is built with 150 + codeFreezeDays + stabilizationDays days from planStart — if epics are scheduled far enough out, dates could fall outside this window (console.warn added)

---

## How to Deploy
```bash
cd "static/app"
npm run build
cd ../..
forge deploy
# forge install  (first time only)
```
